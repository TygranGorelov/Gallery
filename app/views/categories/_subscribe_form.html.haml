- if user_signed_in?
  - if @category.user_id != current_user.id
    - if current_user.present? && @category.subscribed_by?(current_user)
      = link_to 'Unsubscribe', category_subscribe_path(@category.id), method: 'delete', class: 'text-danger'
    - else current_user.present?
      = link_to 'Subscribe', category_subscribe_path(@category.id), method: 'post', class: 'text-success'
%br
Followers:
= @category.followers.count